# Rechnify Logic Prototype Notes

Question: does one in-memory model cover the main Rechnify workflow across customer setup, draft invoice editing, sending, payment, duplication, customer deletion rules, VAT totals, and dashboard aggregates?

How to run:

```bash
npm run prototype:logic
```

This is throwaway code. Keep the reducer shape only if driving the prototype shows that the transitions and derived totals feel right.

Verdict placeholder:

- Winning model:
- Surprising edge cases:
- What should move into production:
- What should be deleted:
